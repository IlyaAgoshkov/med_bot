# Пример файла для переопределения настроек docker-compose
# Скопируйте этот файл в docker-compose.override.yml и настройте под свои нужды

version: '3.8'

services:
  med_bot:
    # Пример: изменить политику перезапуска
    # restart: always
    
    # Пример: добавить дополнительные переменные окружения
    # environment:
    #   - DEBUG=1
    
    # Пример: изменить порты (если понадобится в будущем)
    # ports:
    #   - "8080:8080"
